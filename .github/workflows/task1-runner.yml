name: Task 1 branch runner

on:
  push:
    branches:
      - work/task1-hc-revisit-gated

permissions:
  contents: write

jobs:
  apply-generate-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Apply Task 1 transforms
        id: apply
        shell: bash
        run: |
          if [[ ! -f .github/task1.py ]]; then
            echo "applied=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          python .github/task1.py
          rm -f .github/task1.py .github/task1.patch
          echo "applied=true" >> "$GITHUB_OUTPUT"
      - name: Focused validation
        if: steps.apply.outputs.applied == 'true'
        run: |
          node tests/hc-revisit-routes.test.mjs
          node tests/h-economy-balance.test.mjs
          node tests/field-map-export.test.mjs || true
          node tests/source-contracts.test.mjs
      - name: Regenerate developer map and PWA assets
        if: steps.apply.outputs.applied == 'true'
        run: |
          node scripts/export-field-map.mjs
          node scripts/build-precache.mjs
      - name: Freshness and hygiene
        if: steps.apply.outputs.applied == 'true'
        run: |
          node scripts/export-field-map.mjs --check
          node scripts/build-precache.mjs --check
          node scripts/check-repository-hygiene.mjs
          git diff --check
      - name: Commit implementation
        if: steps.apply.outputs.applied == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Gate H/C revisit geometry behind combustion drive"
          git push
