export const NITROGEN_REGION_ID='nitrogen';
export const NITROGEN_REGION_Y=-12850;
export const NITROGEN_REGION_BOUNDS=Object.freeze({left:-1250,right:1350,top:-18150,bottom:500});
export const NITROGEN_ENTRY=Object.freeze({x:280,y:-12920,angle:-Math.PI/2});
// Developer-facing nominal terminus only. Production route/core geometry is
// authored in nitrogen-routes.js and remains the source of truth.
export const NITROGEN_EXIT=Object.freeze({x:0,y:-17700,angle:-Math.PI/2});
